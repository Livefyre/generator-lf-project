# <%= name %>

<%= desc %>

## Local dev

Install deps: ```make```

Start dev server: ```make run```
```http://localhost:808X/sample/sample.html```

Lint: ```make lint```

Run tests: ```make test``` or ```make testw```
